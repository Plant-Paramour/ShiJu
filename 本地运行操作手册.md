# 诗矩本地运行操作手册

以下命令适用于 Windows PowerShell。每次打开新的 PowerShell 窗口后，需要重新执行对应的环境变量命令。

## 本地固定环境

本项目本机默认使用以下 Python 环境：

```text
C:\ProgramData\anaconda3\envs\ShiJu
```

本机 PostgreSQL 开发连接约定如下。该配置仅用于本地开发，不要复制到公网部署环境：

```powershell
$env:DATABASE_URL = "postgresql://postgres:159487@127.0.0.1:5432/shiju"
```

如果通过迁移工具连接本机 PostgreSQL，工具默认使用同一组本地连接参数。

## 一、启动 AI 对话服务

先进入项目目录并激活虚拟环境：

```powershell
conda activate ShiJu
cd C:\code\shiJu
```

然后配置 AI 服务。只需要修改 `SHIJU_LLM_API_KEY`，它是你在 `api.zscc.in` 获取的真实 Key。

```powershell
conda activate ShiJu
cd C:\code\shiJu

$env:SHIJU_WEB_AGENT_ENABLED = "true"

# 你的 OpenAI-compatible 模型服务
$env:SHIJU_LLM_BASE_URL = "https://api.zscc.in/v1"
$env:SHIJU_LLM_API_KEY = "把这里替换成真实API_KEY"
$env:SHIJU_LLM_MODEL = "deepseek-v3.2-guiji-cc"

# 诗矩本地登录令牌签名密钥，不是模型 API Key
# 可以自己改成任意足够长的随机字符串
$env:SHIJU_AUTH_SECRET = "shiju-local-auth-secret-请改成你自己的随机字符串"

# 控制面和 Worker 的内部认证令牌，仅在本机服务之间使用
$env:SHIJU_AGENT_TOKEN = "shiju-local-agent-token"
$env:SHIJU_WORKER_TOKEN = "shiju-local-worker-token"

# 使用默认数据库
$env:SHIJU_DATABASE_PATH = "var/shiju.db"
```

启动 FastAPI：

```powershell
& "C:\ProgramData\anaconda3\envs\ShiJu\python.exe" `
  -m uvicorn apps.api.main:create_app `
  --factory `
  --host 127.0.0.1 `
  --port 8000
```

看到 `Uvicorn running on http://127.0.0.1:8000` 后，在浏览器打开：

<http://127.0.0.1:8000/web/prosody-checker/#chat>

测试登录账号：

```text
Test1 / Test1
Test2 / Test2
Test3 / Test3
```

## 二、调用你提供的模型 API

下面命令可以单独测试 `api.zscc.in` 是否可用。请把 Key 替换为真实值：

```powershell
$env:NEW_API_KEY = "把这里替换成真实API_KEY"

curl.exe https://api.zscc.in/v1/chat/completions `
  -H "Authorization: Bearer $env:NEW_API_KEY" `
  -H "Content-Type: application/json" `
  -d '{
    "model": "deepseek-v3.2-guiji-cc",
    "messages": [
      {"role": "user", "content": "请写一句关于春山的古诗"}
    ],
    "stream": false
  }'
```

诗矩使用同一个接口，但会额外发送工具调用配置，用于查询韵书和词牌。上游模型需要支持 OpenAI-compatible 的 `tools` / `tool_calls`。

## 三、配置模型轮换池（可选）

轮换池用于配置多个 OpenAI-compatible 端点。诗矩会优先使用 `primary` 主力池；端点发生超时、连接失败、429 或 5xx 时，在有限次数内切换到同层其他端点，再进入 `secondary` 和 `emergency`。401/403 会让对应端点进入较长冷却期；400、模型不存在、参数不支持等客户端错误不会反复重试。

### 3.1 创建配置文件

复制示例文件，并按实际服务商修改。示例文件已经放了 3 个 API 实例；每个实例是一组 `URL + Key`，通过 `models = [...]` 对应多个模型。

```powershell
Copy-Item .\deploy\model-gateway.toml.example .\var\model-gateway.toml
```

配置文件只保存端点信息，不保存明文 Key。每个端点必须把服务商、URL、模型和 Key 引用作为一个完整组合：

```toml
[[endpoints]]
provider_id = "api-1"
base_url = "https://你的服务1/v1"       # URL
secret_ref = "SHIJU_LLM_API_1_KEY"      # Key 对应的环境变量名
models = ["deepseek-v3.2-guiji-cc", "deepseek-v4-flash-nv-cc", "Qwen/Qwen3.5-9B", "Qwen/Qwen3.6-27B"]
tier = "primary"
priority = 10
weight = 1
max_concurrency = 8
timeout_seconds = 90
supports_tools = true
supports_stream = true
cost_level = "standard"

[[endpoints]]
provider_id = "api-2"
base_url = "https://你的服务2/v1"       # URL
secret_ref = "SHIJU_LLM_API_2_KEY"      # Key 对应的环境变量名
models = ["deepseek-v3.2-guiji-cc", "deepseek-v4-flash-nv-cc", "Qwen/Qwen3.5-9B", "Qwen/Qwen3.6-27B"]
tier = "primary"
priority = 20
weight = 1
max_concurrency = 8
timeout_seconds = 90
supports_tools = true
supports_stream = true
cost_level = "standard"

[[endpoints]]
provider_id = "api-3"
base_url = "https://你的服务3/v1"       # URL
secret_ref = "SHIJU_LLM_API_3_KEY"      # Key 对应的环境变量名
models = ["deepseek-v3.2-guiji-cc", "deepseek-v4-flash-nv-cc", "Qwen/Qwen3.5-9B", "Qwen/Qwen3.6-27B"]
tier = "secondary"
priority = 10
weight = 1
max_concurrency = 4
timeout_seconds = 120
supports_tools = true
supports_stream = true
cost_level = "higher"
```

然后在同一个 PowerShell 窗口填写 3 个 Key：

```powershell
$env:SHIJU_LLM_API_1_KEY = "服务1的真实Key"
$env:SHIJU_LLM_API_2_KEY = "服务2的真实Key"
$env:SHIJU_LLM_API_3_KEY = "服务3的真实Key"
$env:SHIJU_MODEL_GATEWAY_CONFIG = "C:\code\shiJu\var\model-gateway.toml"
```

如果 3 个 Key 都来自 `api.zscc.in`，每个 Key 都可以在同一个端点下配置这 4 个模型。网关会自动展开成 12 个可轮换组合：

```text
SHIJU_LLM_API_1_KEY -> 4 个模型
SHIJU_LLM_API_2_KEY -> 4 个模型
SHIJU_LLM_API_3_KEY -> 4 个模型
```

如果某个组合返回模型不存在、参数不兼容、鉴权失败、限流、超时或 5xx，网关会继续尝试池中的下一个完整组合（URL + Key + Model）。因此每个模型都应配置在真实可用的 URL/Key 组合上；不能只更换 Key 而保留不匹配的 Model。

对应关系就是：

| 池中实例 | URL | Key | Model |
| --- | --- | --- | --- |
| api-1 | `base_url` | `SHIJU_LLM_API_1_KEY` 的值 | `model` |
| api-2 | `base_url` | `SHIJU_LLM_API_2_KEY` 的值 | `model` |
| api-3 | `base_url` | `SHIJU_LLM_API_3_KEY` 的值 | `model` |

字段说明：

- `tier` 只能是 `primary`、`secondary` 或 `emergency`。
- 同一层级中，`priority` 数字越小越优先；相同优先级按 `weight` 加权选择。
- `max_concurrency` 是单端点并发上限，不是全局并发上限。
- Agent 对话需要 `supports_tools = true`；流式对话需要 `supports_stream = true`。
- `secret_ref` 是环境变量名，不是 Key 本身。

### 3.2 注入端点 Key 并启用

在启动 FastAPI 的同一个 PowerShell 窗口中设置：

```powershell
$env:SHIJU_LLM_ZSCC_KEY = "第一个服务商的真实API_KEY"
$env:SHIJU_LLM_BACKUP_KEY = "备用服务商的真实API_KEY"
$env:SHIJU_MODEL_GATEWAY_CONFIG = "C:\code\shiJu\var\model-gateway.toml"
$env:SHIJU_MODEL_GATEWAY_MAX_ATTEMPTS = "3"
```

启用轮换池后，不再需要设置旧的单端点变量 `SHIJU_LLM_BASE_URL`、`SHIJU_LLM_API_KEY`、`SHIJU_LLM_MODEL`。如果没有设置 `SHIJU_MODEL_GATEWAY_CONFIG`，程序会继续使用旧的单端点配置，便于本地兼容运行。

然后按本手册第一节启动 FastAPI。环境变量只对当前 PowerShell 窗口有效，重新打开窗口后需要重新设置。

### 3.3 本地验证

先检查配置文件路径和 Key 是否在当前窗口生效：

```powershell
Test-Path $env:SHIJU_MODEL_GATEWAY_CONFIG
if ($env:SHIJU_LLM_ZSCC_KEY) { "primary key configured" }
if ($env:SHIJU_LLM_BACKUP_KEY) { "backup key configured" }
```

再启动服务并发送一条聊天请求。服务调用的端点不会把 Key 写入日志；轮换池的端点状态可通过 Python 测试或应用内健康检查扩展进行观察。要验证故障切换，应暂时将主端点 URL 改为不可达地址，确认请求最终切换到备用端点；验证完成后恢复配置。

注意：已经向浏览器输出部分流式文本后，如果连接中断，系统会报告“响应中断”而不会自动切换并重复文本。这是为了避免用户看到重复回复。

## 四、启动格律诗词生成 Worker（可选）

只聊天和查询韵书不需要 Worker。要真正生成格律诗词，需要另开一个 PowerShell 窗口，并配置本机模型目录：

```powershell
conda activate ShiJu
cd C:\code\shiJu

$env:SHIJU_CONTROL_URL = "http://127.0.0.1:8000"
$env:SHIJU_WORKER_TOKEN = "shiju-local-worker-token"
$env:SHIJU_WORKER_ID = "local-worker"

# 改成你本机的 Transformers 模型目录
$env:SHIJU_MODEL_PATH = "C:\Users\26051\.cache\modelscope\hub\models\Qwen\Qwen3-4B"
$env:SHIJU_QUANTIZATION = "8bit"
$env:SHIJU_RHYME_DIR = "C:\code\shiJu\Rhyme"
$env:SHIJU_METER_DIR = "C:\code\shiJu\Songci_Meter"

& "C:\ProgramData\anaconda3\envs\ShiJu\python.exe" -m apps.gpu_worker.main
```

如果不启动 Worker，AI 对话仍可使用，但确认生成的诗词任务会停留在队列中。

## 五、使用顺序

1. 启动第一个 PowerShell 中的 FastAPI 服务。
2. 浏览器打开聊天页面并使用 `Test1 / Test1` 登录。
3. 输入创作需求，例如“写一首秋夜思乡的七言绝句”。
4. AI 给出创作方案后，输入“确认生成”。
5. 启动 Worker 后，聊天区会显示可展开的生成进度气泡。
6. 生成完成的诗词会自动出现在“个人中心”。

## 六、常见问题

### 缺少 Agent 配置

确认启动 FastAPI 的 PowerShell 窗口中已经设置了：

```powershell
$env:SHIJU_WEB_AGENT_ENABLED = "true"
$env:SHIJU_LLM_BASE_URL = "https://api.zscc.in/v1"
$env:SHIJU_LLM_API_KEY = "真实API_KEY"
$env:SHIJU_LLM_MODEL = "deepseek-v3.2-guiji-cc"
```

### 登录后无法聊天

确认服务是用最新环境变量启动的，并检查 `/health/live`：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health/live
```

### 任务一直排队

这是 Worker 没有启动，或者 `SHIJU_CONTROL_URL`、`SHIJU_WORKER_TOKEN` 与 API 服务不一致。检查 Worker 窗口是否出现“领取任务失败”。
